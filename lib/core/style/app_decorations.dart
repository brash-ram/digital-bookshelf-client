import 'package:flutter/material.dart';

const defaultValue = 16.0;
const smallestValue = 4.0;
const smallValue = 8.0;
const bigValue = 24.0;
const biggerValue = 32.0;
const biggestValue = 48.0;

const borderedButton = 12.0;

const smallRadius    = BorderRadius.all(Radius.circular(smallValue));
const defaultRadius  = BorderRadius.all(Radius.circular(defaultValue));
const borderedRadius = BorderRadius.all(Radius.circular(borderedButton));